Problem 1

A)The code will not execute it will throw compile time error.
 if a  class is extending an abstract class ,then the must child class must be implemented ot mark the child 
 class as abstract class.Here the child has not implemented anything or it is not mark as abstract class
 so the code will throw an error
 
 B)The code will  execute .
 if a  class is extending an abstract class ,then the must child class must be implemented or mark the child 
 class as abstract class.Here the child has  marked as abstract class
 